[build]
  base = "client"
  publish = "client/dist"
  command = "pnpm install && pnpm build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Optional: if you want Netlify to detect workspace properly
[functions]
  node_bundler = "esbuild"
